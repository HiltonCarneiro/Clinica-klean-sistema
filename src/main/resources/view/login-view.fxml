<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="br.com.clinica.controller.LoginController"
           styleClass="login-root">

    <VBox spacing="16" alignment="CENTER" styleClass="login-card">

        <!-- LOGO -->
        <ImageView fx:id="imgLogoLogin"
                   fitHeight="84"
                   preserveRatio="true"
                   smooth="true"
                   opacity="0.95"/>

        <!-- TÍTULO -->
        <Label text="Clínica Klean - Saúde Integrativa"
               styleClass="login-title"/>

        <!-- FORM -->
        <GridPane hgap="12" vgap="12" alignment="CENTER">
            <columnConstraints>
                <ColumnConstraints minWidth="76" halignment="RIGHT"/>
                <ColumnConstraints minWidth="300" hgrow="NEVER"/>
                <ColumnConstraints minWidth="44" halignment="CENTER"/>
            </columnConstraints>

            <!-- USUÁRIO -->
            <Label text="Usuário:" styleClass="login-label"
                   GridPane.rowIndex="0" GridPane.columnIndex="0"/>

            <TextField fx:id="txtUsuario"
                       prefWidth="300"
                       styleClass="login-field"
                       GridPane.rowIndex="0" GridPane.columnIndex="1"
                       GridPane.columnSpan="2"/>

            <!-- SENHA -->
            <Label text="Senha:" styleClass="login-label"
                   GridPane.rowIndex="1" GridPane.columnIndex="0"/>

            <PasswordField fx:id="txtSenha"
                           prefWidth="300"
                           styleClass="login-field"
                           GridPane.rowIndex="1" GridPane.columnIndex="1"/>

            <TextField fx:id="txtSenhaVisivel"
                       prefWidth="300"
                       styleClass="login-field"
                       visible="false"
                       managed="false"
                       GridPane.rowIndex="1" GridPane.columnIndex="1"/>

            <Button fx:id="btnToggleSenha"
                    text="👁"
                    onAction="#onToggleSenha"
                    styleClass="login-eye"
                    prefWidth="44"
                    GridPane.rowIndex="1" GridPane.columnIndex="2"/>
        </GridPane>

        <Label fx:id="lblErro" styleClass="login-error"/>

        <Button text="Entrar"
                onAction="#onEntrar"
                prefWidth="220"
                styleClass="login-button"/>

    </VBox>
</StackPane>
